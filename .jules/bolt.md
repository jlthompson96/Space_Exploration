## 2026-05-03 - [React Code Splitting]
**Learning:** Top-level route components in this React project are large and independent. Initial bundle sizes can be significantly reduced by applying code splitting.
**Action:** Use `React.lazy()` and `Suspense` for React Router components.
